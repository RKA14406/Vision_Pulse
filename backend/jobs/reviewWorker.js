// Placeholder for Phase 3.
// This file will later calculate post-event actual movement and write accuracy reviews.
// MVP currently uses data/mock_reviews.json so the dashboard can demo the review loop immediately.

export function reviewWorkerPlaceholder() {
  return "Review worker not active in MVP phase.";
}
